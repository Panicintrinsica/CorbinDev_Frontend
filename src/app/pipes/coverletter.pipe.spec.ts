import { CoverletterPipe } from './coverletter.pipe';

describe('CoverletterPipe', () => {
  it('create an instance', () => {
    const pipe = new CoverletterPipe();
    expect(pipe).toBeTruthy();
  });
});
